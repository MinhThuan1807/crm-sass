{
  "name": "Lint BE on Save",
  "version": "1.0.0",
  "description": "Chạy lint tự động khi save file TypeScript trong backend",
  "when": {
    "type": "fileEdited",
    "patterns": ["be/src/**/*.ts"]
  },
  "then": {
    "type": "runCommand",
    "command": "cd be && npm run lint"
  }
}
